<%= simple_form_for @movement, html: { class: "form movements" } do |f| %>
    <%= f.input :name, placeholder: "Name your movement", hint: "Need to be unique" %>
    <%= f.button :submit, class: "btn btn--secondary" %>
<% end %>
