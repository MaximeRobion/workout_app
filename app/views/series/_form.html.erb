<%= simple_form_for [@workout, @exercise, @exercise.series.build], url: workout_exercise_series_index_path(@workout, @exercise) do |f| %>
  <%= f.input :weight %>
  <%= f.input :repetition %>
  <%= f.button :submit %>
<% end %>
