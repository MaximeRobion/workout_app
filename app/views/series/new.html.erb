<div class="container">
    <div class="header">
        <div class="header_heading">
            <%= link_to 'arrow_back', url_for(:back), class: "material-symbols-outlined" %>
            <h1>New serie</h1>
        </div>
    </div>

    <%= turbo_frame_tag nested_dom_id(@exercise, Serie.new) do %>
        <%= render "form", serie: @serie %>
    <% end %>
 </div>

