<h1>Workouts</h1>

<% @workouts.each do |workout| %>
    <%= link_to (workout) do %>
    <p><%= local_time(workout.date, format: '%a %d/%m/%Y') %> - <%= workout.note %></p>
    <% end %>
<% end %>

<button><%= link_to "New Workout", new_workout_path %></button>
